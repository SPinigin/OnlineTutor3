@model OnlineTutor3.Domain.Entities.NotParticleQuestion
@{
    ViewData["Title"] = "Удаление вопроса на правописание частицы \"не\"";
    var test = ViewBag.Test as OnlineTutor3.Domain.Entities.NotParticleTest;
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white py-2">
                <h5 class="mb-0">
                    <i class="fas fa-exclamation-triangle"></i> Удаление вопроса
                </h5>
            </div>
            <div class="card-body p-3">
                <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle"></i> 
                    <strong>Внимание!</strong> Вы уверены, что хотите удалить этот вопрос? Это действие нельзя отменить.
                </div>

                @if (test != null)
                {
                    <div class="mb-3">
                        <strong>Тест:</strong> @test.Title
                    </div>
                }

                <div class="mb-3">
                    <strong>Порядковый номер:</strong> @Model.OrderIndex
                </div>

                <div class="mb-3">
                    <strong>Текст с (не):</strong> @Model.TextWithGap
                </div>

                <div class="mb-3">
                    <strong>Правильный ответ:</strong> @Model.CorrectAnswer
                </div>

                <div class="mb-3">
                    <strong>Полный текст:</strong> @Model.FullText
                </div>

                <div class="mb-3">
                    <strong>Баллы:</strong> @Model.Points
                </div>

                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex justify-content-end gap-2 mt-3">
                        <a asp-controller="NotParticleTest" asp-action="Details" asp-route-id="@Model.NotParticleTestId" class="btn btn-sm btn-outline-secondary">
                            <i class="fas fa-arrow-left"></i> Отмена
                        </a>
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="fas fa-trash"></i> Удалить вопрос
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

