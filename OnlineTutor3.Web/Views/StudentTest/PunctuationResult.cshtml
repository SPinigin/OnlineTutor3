@model OnlineTutor3.Web.ViewModels.PunctuationTestResultViewModel
@{
    ViewData["Title"] = $"Результат теста: {Model.TestTitle}";
    
    var gradeColor = Model.Percentage >= 80 ? "success" :
                     Model.Percentage >= 60 ? "warning" :
                     Model.Percentage >= 40 ? "info" : "danger";
}

@if (!(ViewBag.IsModal as bool? ?? false))
{
    <div class="mb-3">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
            <nav aria-label="breadcrumb" class="flex-grow-1">
                <ol class="breadcrumb breadcrumb-compact mb-0">
                    <li class="breadcrumb-item">
                        <a asp-controller="StudentTest" asp-action="History">История прохождения</a>
                    </li>
                    <li class="breadcrumb-item active text-truncate">@Model.TestTitle</li>
                </ol>
            </nav>
            <a asp-controller="StudentTest" asp-action="Index" class="btn btn-modern-outline-primary btn-sm">
                <i class="fas fa-list"></i>
                <span>К тестам</span>
            </a>
        </div>
    </div>
}

<div class="test-container">
    <div class="card modern-card mb-2">
        <div class="card-header modern-card-header" style="background: linear-gradient(135deg, #ff9800 0%, #ffc107 100%); color: white;">
            <div class="d-flex justify-content-between align-items-center gap-2">
                <div class="flex-grow-1 min-w-0">
                    <h6 class="mb-0 d-flex align-items-center" style="font-size: 0.9rem; color: white;">
                        <i class="fas @Model.TestIcon me-2 flex-shrink-0"></i>
                        <span class="text-truncate">@Model.TestTitle</span>
                    </h6>
                </div>
            </div>
        </div>
    </div>

    <div class="card modern-card mb-2">
        <div class="card-body p-0">
            <div class="row text-center g-0">
                <div class="col-4">
                    <div class="d-flex flex-column align-items-center">
                        <div class="result-circle mb-0" style="width: 160px; height: 160px; position: relative;">
                            <svg viewBox="0 0 100 100" style="width: 100%; height: 100%;">
                                <circle cx="50" cy="50" r="32" fill="none" stroke="#e5e7eb" stroke-width="4" />
                                <circle cx="50" cy="50" r="32" fill="none"
                                        stroke="#6b7280"
                                        stroke-width="5"
                                        stroke-linecap="round"
                                        stroke-dasharray="@(Model.Percentage * 2.01), 201"
                                        transform="rotate(-90 50 50)" />
                                <text x="50" y="45" text-anchor="middle" dominant-baseline="middle"
                                      font-size="16" font-weight="800"
                                      fill="#6b7280">
                                    @Model.Percentage.ToString("F1")%
                                </text>
                                <text x="50" y="60" text-anchor="middle" dominant-baseline="middle"
                                      font-size="9" font-weight="600"
                                      fill="#6b7280">
                                    @Model.Score / @Model.MaxScore
                                </text>
                            </svg>
                        </div>
                        <div style="margin-top: 1px;">
                            <span style="font-size: 11px; font-weight: 600; color: #374151; text-transform: uppercase; letter-spacing: 0.5px;">Баллы</span>
                        </div>
                    </div>
                </div>
                
                <div class="col-4">
                    <div class="d-flex flex-column align-items-center">
                        <div class="result-circle mb-0" style="width: 160px; height: 160px; position: relative;">
                            <svg viewBox="0 0 100 100" style="width: 100%; height: 100%;">
                                @{
                                    var gradeCircleColor = Model.Grade switch
                                    {
                                        5 => "#43e97b",
                                        4 => "#4facfe",
                                        3 => "#ff9800",
                                        2 => "#f44336",
                                        _ => "#6c757d"
                                    };
                                }
                                <circle cx="50" cy="50" r="32" fill="none" stroke="#e5e7eb" stroke-width="4" />
                                <circle cx="50" cy="50" r="32" fill="none"
                                        stroke="@gradeCircleColor"
                                        stroke-width="5"
                                        stroke-linecap="round"
                                        stroke-dasharray="201, 201"
                                        transform="rotate(-90 50 50)" />
                                <text x="50" y="55" text-anchor="middle" dominant-baseline="middle"
                                      font-size="43" font-weight="900"
                                      fill="@gradeCircleColor">
                                    @Model.Grade
                                </text>
                            </svg>
                        </div>
                        <div style="margin-top: 1px;">
                            <span style="font-size: 11px; font-weight: 600; color: #374151; text-transform: uppercase; letter-spacing: 0.5px;">Оценка</span>
                        </div>
                    </div>
                </div>
                
                <div class="col-4">
                    <div class="d-flex flex-column align-items-center">
                        <div class="result-circle mb-0" style="width: 160px; height: 160px; position: relative;">
                            <svg viewBox="0 0 100 100" style="width: 100%; height: 100%;">
                                @{
                                    var totalSeconds = Model.Duration.TotalSeconds;
                                    var maxSeconds = Model.Test.TimeLimit * 60; // Лимит времени теста в секундах
                                    var timePercentage = maxSeconds > 0 ? Math.Min(100, (totalSeconds / maxSeconds) * 100) : 0;
                                }
                                <defs>
                                    <linearGradient id="grad3-@Model.TestResult.Id" x1="0%" y1="0%" x2="100%" y2="100%">
                                        <stop offset="0%" style="stop-color:#8b5cf6;stop-opacity:1" />
                                        <stop offset="100%" style="stop-color:#a78bfa;stop-opacity:1" />
                                    </linearGradient>
                                </defs>
                                <circle cx="50" cy="50" r="32" fill="none" stroke="#e5e7eb" stroke-width="4" />
                                <circle cx="50" cy="50" r="32" fill="none"
                                        stroke="url(#grad3-@Model.TestResult.Id)"
                                        stroke-width="5"
                                        stroke-linecap="round"
                                        stroke-dasharray="@(timePercentage * 2.01), 201"
                                        transform="rotate(-90 50 50)" />
                                <text x="50" y="55" text-anchor="middle" dominant-baseline="middle"
                                      font-size="16" font-weight="800"
                                      fill="#8b5cf6">
                                    @($"{Model.Duration.Minutes:D2}:{Model.Duration.Seconds:D2}")
                                </text>
                            </svg>
                        </div>
                        <div style="margin-top: 1px;">
                            <span style="font-size: 11px; font-weight: 600; color: #374151; text-transform: uppercase; letter-spacing: 0.5px;">Время</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if (Model.ShowCorrectAnswers)
    {
        <div class="card modern-card mb-2">
            <div class="card-header modern-card-header py-1">
                <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
                    <h6 class="mb-0 modern-card-title" style="font-size: 0.85rem;">
                        <i class="fas fa-list-alt nav-link-icon"></i> Детальные результаты
                    </h6>
                    
                    <div class="form-check form-switch mb-0">
                        <input class="form-check-input" type="checkbox" id="showErrorsOnly" checked style="font-size: 0.8rem;">
                        <label class="form-check-label text-dark-modern" for="showErrorsOnly" style="font-size: 0.75rem;">
                            Только ошибки
                        </label>
                    </div>
                </div>
            </div>
            <div class="card-body p-1 p-md-2">
                @for (int i = 0; i < Model.Questions.Count; i++)
                {
                    var question = Model.Questions[i];
                    var answer = Model.Answers.FirstOrDefault(a => a.PunctuationQuestionId == question.Id);
                    var isCorrect = answer?.IsCorrect ?? false;

                    <div class="question-result mb-1 p-1 border rounded @(isCorrect ? "border-success" : "border-danger")"
                         data-correct="@isCorrect.ToString().ToLower()">
                        <div class="d-flex align-items-start gap-1">
                            <div class="flex-shrink-0">
                                <span class="badge @(isCorrect ? "badge-modern-success" : "badge-modern-danger")" style="font-size: 0.7rem;">@(i + 1)</span>
                            </div>
                            
                            <div class="flex-grow-1 min-w-0">
                                <div class="mb-1 py-1 px-2 bg-light rounded">
                                    <small class="d-block" style="font-size: 0.9rem; color: #856404;">@question.SentenceWithNumbers</small>
                                </div>
                                
                                <div class="row g-1">
                                    <div class="col-6">
                                        <small class="text-muted-modern d-block" style="font-size: 0.7rem;">Ваш ответ:</small>
                                        <div class="fw-bold @(isCorrect ? "nav-link-icon-green" : "nav-link-icon-danger")" style="font-size: 0.75rem;">
                                            @if (!string.IsNullOrEmpty(answer?.StudentAnswer))
                                            {
                                                <span>@answer.StudentAnswer</span>
                                            }
                                            else
                                            {
                                                <span class="text-muted-modern">Не отвечено</span>
                                            }
                                        </div>
                                    </div>
                                    <div class="col-6">
                                        <small class="text-muted-modern d-block" style="font-size: 0.7rem;">Правильный:</small>
                                        <div class="fw-bold nav-link-icon-green" style="font-size: 0.75rem;">
                                            @if (!string.IsNullOrEmpty(question.CorrectPositions))
                                            {
                                                <span>@question.CorrectPositions</span>
                                            }
                                            else
                                            {
                                                <span>Без запятых</span>
                                            }
                                        </div>
                                    </div>
                                </div>

                                @if (!string.IsNullOrEmpty(question.PlainSentence))
                                {
                                    <div class="mt-1 py-1 px-2 bg-success bg-opacity-10 rounded border-start border-success border-2">
                                        <small class="d-block text-muted" style="font-size: 0.7rem;">Правильный вариант:</small>
                                        <small class="fw-bold d-block" style="font-size: 0.8rem;">@question.PlainSentence</small>
                                    </div>
                                }

                                @if (!string.IsNullOrEmpty(question.Hint))
                                {
                                    <div class="mt-1 py-1 px-2 modern-alert-info rounded" style="background: linear-gradient(135deg, rgba(79, 172, 254, 0.1) 0%, rgba(0, 242, 254, 0.1) 100%); border-left: 3px solid #4facfe;">
                                        <small class="d-block" style="font-size: 0.7rem;">
                                            <i class="fas fa-lightbulb nav-link-icon-blue"></i>
                                            <span class="text-dark-modern">@question.Hint</span>
                                        </small>
                                    </div>
                                }
                            </div>
                            
                            <div class="flex-shrink-0 text-center">
                                @if (isCorrect)
                                {
                                    <i class="fas fa-check-circle nav-link-icon-green fa-lg"></i>
                                    <div class="mt-0">
                                        <small class="badge badge-modern-success" style="font-size: 0.65rem;">@question.Points</small>
                                    </div>
                                }
                                else
                                {
                                    <i class="fas fa-times-circle nav-link-icon-danger fa-lg"></i>
                                    <div class="mt-0">
                                        <small class="badge badge-modern-danger" style="font-size: 0.65rem;">0</small>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        <div class="card modern-card mb-2">
            <div class="card-body p-2 text-center">
                <p class="text-muted-modern mb-0" style="font-size: 0.85rem;">
                    <i class="fas fa-info-circle nav-link-icon"></i>
                    Детальные результаты недоступны. Учитель отключил показ правильных ответов.
                </p>
            </div>
        </div>
    }
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Применяем фильтр при загрузке страницы
            function applyFilter() {
                if ($('#showErrorsOnly').is(':checked')) {
                    $('.question-result[data-correct="true"]').hide();
                } else {
                    $('.question-result').show();
                }
            }
            
            // Применяем фильтр сразу при загрузке
            applyFilter();
            
            // Обработчик изменения переключателя
            $('#showErrorsOnly').on('change', function() {
                applyFilter();
            });
        });
    </script>
}

