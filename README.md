# OnlineTutor3 - Новое приложение

## Описание

OnlineTutor3 - это полностью независимое приложение, созданное с нуля на основе функциональности OnlineTutor2, но с улучшенной архитектурой и логикой.

## Структура проекта

Проект состоит из 4 слоев (Clean Architecture):

```
OnlineTutor3/
├── OnlineTutor3.Domain/          # Доменные модели и интерфейсы
├── OnlineTutor3.Application/     # Бизнес-логика и сервисы
├── OnlineTutor3.Infrastructure/  # Инфраструктура (БД, репозитории)
└── OnlineTutor3.Web/             # Веб-приложение (контроллеры, Views)
```

## Технологии

- .NET 10.0
- ASP.NET Core MVC
- Entity Framework Core (только для Identity)
- ADO.NET (для бизнес-логики)
- ASP.NET Core Identity
- NLog

## Архитектурные принципы

1. **Разделение ответственности**: SQL-запросы только в репозиториях, НЕ в контроллерах
2. **Слоистая архитектура**: Domain → Application → Infrastructure → Web
3. **Dependency Injection**: Все зависимости через DI контейнер
4. **Прямые SQL-запросы**: Использование ADO.NET для работы с БД

## Настройка базы данных

### Важно! Перед первым запуском:

1. **Создайте базу данных** - выполните SQL-скрипт:
   ```
   OnlineTutor3.Infrastructure/Data/CreateDatabase.sql
   ```
   Этот скрипт создаст базу данных `OnlineTutor3` и таблицу `Subjects` с начальными данными.

2. **Проверьте строку подключения** в `appsettings.json`:
   ```json
   "ConnectionStrings": {
     "DefaultConnection": "Server=YOUR_SERVER;Initial Catalog=OnlineTutor3;Trusted_Connection=True;TrustServerCertificate=True"
   }
   ```

3. **При первом запуске** приложение автоматически:
   - Создаст таблицы Identity (AspNetUsers, AspNetRoles и т.д.) через EF Core миграции
   - Создаст роли: Admin, Teacher, Student

### Миграции EF Core

Миграции для Identity уже созданы:
- `InitialIdentity` - создает все таблицы Identity

Если нужно создать новую миграцию:
```bash
cd OnlineTutor3.Infrastructure
dotnet ef migrations add MigrationName --startup-project ..\OnlineTutor3.Web\OnlineTutor3.Web.csproj --context ApplicationDbContext
```

## Запуск приложения

1. Откройте решение `OnlineTutor3.sln`
2. Восстановите NuGet пакеты
3. **Выполните SQL-скрипт** `CreateDatabase.sql` для создания БД
4. Запустите проект `OnlineTutor3.Web`

## Этапы разработки

### ✅ Этап 1: Базовая структура проекта
- Создана структура проекта (4 слоя)
- Настроено подключение к БД
- Настроен Identity
- Созданы базовые модели (ApplicationUser, Subject)
- Созданы базовые Views
- Создана миграция для Identity таблиц

### 🔄 Следующие этапы:
- Этап 2: Создание моделей (Class, Student, Teacher, TeacherSubject)
- Этап 3: Создание репозиториев с SQL-запросами
- Этап 4: Создание сервисов (бизнес-логика)
- Этап 5: Создание контроллеров Account
- Этап 6: Создание Views для аутентификации
- Этап 7: Функциональность тестов
- И т.д.

## Важные правила

1. **SQL-запросы только в репозиториях** - контроллеры НЕ должны содержать SQL
2. **Контроллеры вызывают только сервисы** - вся бизнес-логика в сервисах
3. **Сервисы используют репозитории** - для работы с БД

## Решение проблем

### Ошибка "Invalid object name 'AspNetRoles'"

Эта ошибка возникает, если база данных не создана или миграции не применены.

**Решение:**
1. Убедитесь, что база данных `OnlineTutor3` создана (выполните `CreateDatabase.sql`)
2. При первом запуске приложение автоматически применит миграции
3. Если ошибка повторяется, примените миграции вручную:
   ```bash
   cd OnlineTutor3.Infrastructure
   dotnet ef database update --startup-project ..\OnlineTutor3.Web\OnlineTutor3.Web.csproj --context ApplicationDbContext
   ```

## Лицензия

Внутренний проект
